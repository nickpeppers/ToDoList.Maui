<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ToDoList.Maui.ViewModels"
             xmlns:models="clr-namespace:ToDoList.Maui.Models"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="ToDoList.Maui.Pages.ToDoListPage"
             Title="To Do">
    <ContentPage.BindingContext>
        <vm:ToDoListViewModel x:Name="_viewModel"/>
    </ContentPage.BindingContext>
    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:IsStringNotNullOrEmptyConverter x:Key="_isStringNullOrEmptyConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    <Grid RowDefinitions="1, *, Auto">
        <BoxView VerticalOptions="Start" HeightRequest="1"/>
        <CollectionView Grid.Row="1" ItemsSource="{Binding ToDoItems}" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="{x:Type models:ToDoItem}">
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItem Text="Delete" BackgroundColor="Red" Command="{Binding Source={x:Reference _viewModel}, Path=RemoveItemCommand}" CommandParameter="{Binding .}"/>
                        </SwipeView.RightItems>
                        <Grid RowDefinitions="Auto, Auto, Auto, Auto, 1" ColumnDefinitions="*, Auto" RowSpacing="0">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={x:Reference _viewModel}, Path=ModifyToDoItemCommand}" CommandParameter="{Binding .}"/>
                            </Grid.GestureRecognizers>
                            <Label Margin="8,8" Text="Task" FontSize="Large"/>
                            <Label Grid.Row="1" Margin="8,8" Text="{Binding Title}" IsVisible="{Binding Title, Converter={StaticResource _isStringNullOrEmptyConverter}}" MaxLines="1" FontSize="Large" LineBreakMode="TailTruncation"/>
                            <Label Grid.Row="2" Margin="8,8" Text="Description" IsVisible="{Binding Description, Converter={StaticResource _isStringNullOrEmptyConverter}}" FontSize="Large"/>
                            <Label Grid.Row="3" Margin="8,8" Text="{Binding Description}" IsVisible="{Binding Description, Converter={StaticResource _isStringNullOrEmptyConverter}}" 
                                   MaxLines="2" FontSize="Medium" LineBreakMode="TailTruncation"/>
                            <CheckBox Grid.RowSpan="5" Grid.Column="1" Margin="8" IsChecked="{Binding Completed}" VerticalOptions="Center" HorizontalOptions="Center"/>
                            <BoxView Grid.Row="4" Grid.ColumnSpan="2" VerticalOptions="End" HeightRequest="1"/>
                        </Grid>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button Grid.Row="2" Margin="8" Text="Add Task" Command="{Binding AddToDoItemCommand}"/>
    </Grid>
</ContentPage>